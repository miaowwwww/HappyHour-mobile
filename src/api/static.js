/* 
 * [].find( (item, index) => {}) 的一次封装
 * @params params: object 
 * return object: 第一个匹配params中的所有属性key-value
 * */
Array.prototype.ggfind = (parmas) => {
	let result = this.find((item, index) => {
		// 
	})
}
